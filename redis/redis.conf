# Redis configuration for CBT stack (Docker production compose).
bind 0.0.0.0
port 6379
protected-mode yes
tcp-keepalive 300
timeout 0
daemonize no
supervised no
dir /data
appendonly yes
appendfsync everysec
maxmemory-policy allkeys-lru
